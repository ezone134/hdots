#!/usr/bin/env luajit
-- array dirs_master_array
-- ported from bash sources/dirs_master_array
-- master dirs array; hypr-init creates every dir listed here

local home = os.getenv("HOME") or ""
local lcl_dir = (light_color_list or ""):match("^(.*)/[^/]*$")
local dcl_dir = (dark_color_list or ""):match("^(.*)/[^/]*$")

dirs_array = {
    states,
    home .. "/Downloads",
    hypr_conf,
    hypr_log,
    hypr_scripts,
    hypr_global,
    hypr_global .. "/icons",
    hypr_global .. "/states",
    hypr_conf .. "/rofi",
    hypr_conf .. "/desktop_launchers",
    lock_files,
    hypr_conf .. "/hypr",
    hypr_conf .. "/hypr_cache",
    theme_elements,
    home .. "/.config/hypr",
    hypr_conf .. "/matugen",
    hypr_conf .. "/hypr-theme",
    hypr_conf .. "/alpha-refs",
    hypr_conf .. "/hypr/hyprlock",
    lcl_dir,
    dcl_dir,
    hypr_temp,
    hdots .. "/hypr_cache/accent_files",
    hypr_cache .. "/accent_files",
    hdots .. "/states_global",
    home .. "/.config/rofi",
    hypr_conf .. "/mako",
    hypr_conf .. "/hex_to_alpha",
    hypr_conf .. "/shaders/saturations",
    hypr_conf .. "/shaders/gamma",
}