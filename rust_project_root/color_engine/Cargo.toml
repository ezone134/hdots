[package]
name = "color_engine"
version = "0.1.0"
edition = "2021"
description = "Merged color engine: pick dominant color from a wallpaper AND generate accent shade ramps (Rust ports of color_from_wallpaper_engine + gen_hex_shades)."
license = "MIT"

[[bin]]
name = "color_engine"
path = "src/main.rs"

[dependencies]
image = "0.25"