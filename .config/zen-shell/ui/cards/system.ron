(
    // system card — declared here in the config tree; dynamic body drawn
    // by the resident \`system\` Ink painter (Rust).
    items: [
        Ink(x: 0.0, y: 0.0, w: 0.0, h: 0.0, name: "system"),
    ],
)
