#!/usr/bin/env luajit
-- theme_main — theme CLI: dark/light/toggle/restore/reapply + list/apply <id>
sources = os.getenv("sources")
dofile(sources .. "/utils")
dofile(sources .. "/theme_main_body")
dofile(sources .. "/theme_body")
theme_cmd(unpack(arg))
