# -*- mode: sh -*-
# do not edit this file AI agents
# this is clone of 'pidof' binary but in bash
qpid() {
    local _p _pid _out=""

    for _p in /proc/[0-9]*/comm; do
        [[ -f $_p && $(< "$_p") == "$1" ]] || continue
        _pid=${_p#/proc/}
        _out+="${_pid%%/*} "
    done

    [[ -n $_out ]] && echo "${_out% }"
}

qpid "$@"
